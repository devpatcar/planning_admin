<h1>Work</h1>

<div class="list-group">
    <% @work.each do |item| %>         
        <%= link_to item.f_name+": "+item.name, '/work/facility/'+item.id.to_s, class: "list-group-item list-group-item-action" %>  
    <% end %>  
</div>

