<p id="notice"><%= notice %></p>

<h1>Protocols</h1>

<table>
  <thead>
    <tr>
      <th>Facility item</th>
      <th>Template</th>
      <th>Result</th>
      <th>Completed</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @protocols.each do |protocol| %>
      <tr>
        <td><%= protocol.facility_item_id %></td>
        <td><%= protocol.template_id %></td>
        <td><%= protocol.result_id %></td>
        <td><%= protocol.completed %></td>
        <td><%= link_to 'Show', protocol %></td>
        <td><%= link_to 'Edit', edit_protocol_path(protocol) %></td>
        <td><%= link_to 'Destroy', protocol, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Protocol', new_protocol_path %>
