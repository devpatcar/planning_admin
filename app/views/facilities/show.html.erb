<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @facility.name %>
  <strong>Items</strong>
  <% @facility.facility_items.each do |item| %>
    <%= link_to  item.name, item %>
  <% end %>
</p>

<%= link_to 'Edit', edit_facility_path(@facility) %> |
<%= link_to 'Back', facilities_path %>
